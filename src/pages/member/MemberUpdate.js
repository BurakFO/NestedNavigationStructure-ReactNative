import React from "react";
import { SafeAreaView, StyleSheet, Text, View } from "react-native";

function MemberUpdate() {
    return (

        <SafeAreaView>
            <Text style={{color:'white', fontSize:20}}>Welcome to MemberUpdateScreen</Text>
        </SafeAreaView>
    );
};


export default MemberUpdate;